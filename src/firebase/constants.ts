export const COLLECTIONS = {
  USERS: "users",
  ROOMS: "rooms",
  MESSAGES: "messages",
  LOGS: "logs",
  REPORTS: "reports"
} as const;

export const ROLES = {
  TEACHER: "teacher",
  STUDENT: "student"
} as const;
